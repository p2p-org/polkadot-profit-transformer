version: '3'

networks:
  default:
    external:
      name: streamer_network

services:
  moonbeam:
    build: ./main
    restart: always
    ports:
      - 3000:3000
    depends_on:
      - db
    env_file:
      - docker/env/.moonbeam.env
