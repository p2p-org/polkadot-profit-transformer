version: '3'

networks:
  default:
    external:
      name: streamer_network

services:
  moonbeam:
    build: ./main
    restart: always
    ports:
      - 3003:3001
    env_file:
      - docker/env/.moonbeam.env
